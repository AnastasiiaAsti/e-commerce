// here define all the application level states and define actions to make changes to the state

export const initialState = {
    basket: [],
}

// Selector

const reducer = (state, action) => {

}

export default reducer;